# CourseHub 本地启动服务指南

本文档适用于当前仓库 `C:\Users\Patrick\Desktop\CourseHub`:

- `EchoMind/`:Python/FastAPI 后端(目录沿用旧名,服务已是 CourseHub)
- `frontend/`:React + assistant-ui 前端(构建进 nginx 镜像)

## 1. 服务地址

| 服务 | 地址 | 说明 |
| --- | --- | --- |
| **前端(生产形态)** | <http://localhost> | ChatGPT 式聊天界面,nginx 提供 |
| 开发者面板 | <http://localhost/dev> | 知识库/monitor/skills;也可从侧边栏底部 "Developer" 进入 |
| 前端(开发热更) | <http://localhost:5173> | `npm run dev`,见第 4 节 |
| Python API | <http://localhost:8010> | FastAPI 直连(见下方端口说明) |
| Swagger | <http://localhost:8010/docs> | API 文档 |
| ChromaDB | <http://localhost:8001> | 向量数据库 |
| Prometheus | <http://localhost:9090> | 监控页面 |
| Redis | `localhost:6379` | 工作记忆 |

本机 `8000` 已被其他容器(autofigure-edit)占用,因此 API 宿主机端口用 `8010`(由 `COURSEHUB_HOST_PORT` 控制,容器内仍是 8000)。建议把 `COURSEHUB_HOST_PORT=8010` 写进 `EchoMind/.env`。

## 2. 启动前准备

1. 启动 Docker Desktop,等待 Engine 就绪。
2. 确认 `EchoMind/.env` 已配置(不要提交真实 Key):

```env
ANTHROPIC_BASE_URL=https://api.deepseek.com/anthropic
ANTHROPIC_MODEL=deepseek-v4-flash
ANTHROPIC_API_KEY=你的_DeepSeek_API_Key
COURSEHUB_HOST_PORT=8010
```

## 3. 一条命令起全套(含前端)

```powershell
Set-Location "C:\Users\Patrick\Desktop\CourseHub\EchoMind"
docker compose up -d --build
Start-Process "http://localhost"
```

nginx 镜像内含前端静态产物;改了 `frontend/` 或后端代码后,重跑同一条命令即可(只想重建某个服务:`docker compose up -d --build coursehub` 或 `nginx`)。

正常时以下容器应显示 `healthy`:`coursehub-app`、`coursehub-redis`、`coursehub-chromadb`、`coursehub-prometheus`、`coursehub-nginx`。

## 4. 前端开发热更流程

```powershell
Set-Location "C:\Users\Patrick\Desktop\CourseHub\frontend"
npm install        # 首次
npm run dev        # http://localhost:5173,经 /api 代理到后端
```

后端端口不是 8000 时,建 `frontend/.env.local`:

```env
VITE_PROXY_TARGET=http://localhost:8010
```

测试与类型检查:`npm test`、`npm run typecheck`。

## 5. 修改配置后如何重启

`docker compose restart` **不会重新读取 `.env`**。改 Key 或环境变量后必须重建:

```powershell
Set-Location "C:\Users\Patrick\Desktop\CourseHub\EchoMind"
docker compose up -d --force-recreate coursehub nginx
```

改后端代码:`docker compose up -d --build coursehub`。
改前端代码(生产形态):`docker compose up -d --build nginx`。

## 6. 健康检查与日志

```powershell
Invoke-RestMethod "http://localhost:8010/health"   # API 直连
Invoke-RestMethod "http://localhost/health"        # 经 nginx
Invoke-RestMethod "http://localhost/api/knowledge/stats"
```

```powershell
Set-Location "C:\Users\Patrick\Desktop\CourseHub\EchoMind"
docker compose logs -f --tail 100 coursehub   # 后端
docker compose logs -f --tail 100 nginx       # nginx/前端
```

## 7. 停止服务

```powershell
Set-Location "C:\Users\Patrick\Desktop\CourseHub\EchoMind"
docker compose stop    # 仅停止,保留容器和数据
docker compose down    # 删除容器,保留数据卷
```

不要随意 `docker compose down -v`:`-v` 会删除 Redis、ChromaDB 和 Prometheus 数据卷。

## 8. 常见问题

### 页面打不开或 502

1. `docker compose ps` 看 `coursehub-app` 是否 `healthy`(首次启动要构建课程索引,`start_period` 最长 5 分钟)。
2. `http://localhost/health` 确认 nginx→后端链路。

### 对话报错或日志出现 401

DeepSeek API Key 未生效。改 `EchoMind/.env` 后按第 5 节 `--force-recreate`,不能只 `restart`。

### 端口被占用

`COURSEHUB_HOST_PORT` 换一个宿主机端口即可,不要为了腾端口停掉不相关容器。

### Docker 命令无法连接

先启动 Docker Desktop,等 Engine running 再执行。
