# EchoMind 本地启动服务指南

本文档适用于当前工作区：

- `EchoMind/`：Python/FastAPI 后端
- `EchoMindFrontend/`：Vue 前端
- Java 后端已删除，无需启动

## 1. 服务地址

| 服务 | 地址 | 说明 |
| --- | --- | --- |
| 前端 | <http://localhost:5174> | EchoMind Console |
| Python API | <http://localhost:8003> | FastAPI 直连地址 |
| Swagger | <http://localhost:8003/docs> | API 文档 |
| Nginx | <http://localhost> | Python 后端反向代理 |
| ChromaDB | <http://localhost:8001> | 向量数据库 |
| Prometheus | <http://localhost:9090> | 监控页面 |
| Redis | `localhost:6379` | 工作记忆 |

Python API 使用宿主机端口 `8003`，是因为本机 `8000` 已被其他容器占用。容器内部仍使用 `8000`。

## 2. 启动前准备

1. 启动 Docker Desktop，并等待 Docker Engine 就绪。
2. 打开 PowerShell。
3. 进入工作区根目录：

```powershell
Set-Location "C:\Users\Patrick\Desktop\EchoMind所有代码+简历"
```

4. 确认 `EchoMind/.env` 中已配置 DeepSeek：

```env
ANTHROPIC_BASE_URL=https://api.deepseek.com/anthropic
ANTHROPIC_MODEL=deepseek-v4-flash
ANTHROPIC_API_KEY=你的_DeepSeek_API_Key
ECHOMIND_HOST_PORT=8003
```

不要把真实 API Key 提交到 Git。

## 3. 日常快速启动

已经构建过镜像时，通常只需执行：

```powershell
Set-Location "C:\Users\Patrick\Desktop\EchoMind所有代码+简历"

# 启动 Python 后端、Redis、ChromaDB、Prometheus 和 Nginx
docker compose --project-directory .\EchoMind `
  -f .\EchoMind\docker-compose.yml `
  --env-file .\EchoMind\.env `
  up -d

# 启动前端
docker compose --project-directory .\EchoMindFrontend `
  -f .\EchoMindFrontend\docker-compose.yml `
  up -d
```

启动后打开：

```powershell
Start-Process "http://localhost:5174"
```

## 4. 首次启动或完整重建

### 4.1 构建并启动 Python 后端

```powershell
Set-Location "C:\Users\Patrick\Desktop\EchoMind所有代码+简历"

docker compose --project-directory .\EchoMind `
  -f .\EchoMind\docker-compose.yml `
  --env-file .\EchoMind\.env `
  up -d --build
```

### 4.2 构建并启动前端

前端 Dockerfile 会复制 `dist/`，因此必须先执行前端构建：

```powershell
Set-Location "C:\Users\Patrick\Desktop\EchoMind所有代码+简历\EchoMindFrontend"

# 首次启动或依赖变更时执行
npm ci

# 每次前端源码变更后执行
npm run build

docker compose up -d --build
```

## 5. 修改配置后如何重启

### 5.1 修改 `.env` 或 DeepSeek API Key

`docker compose restart` **不会重新读取 `.env`**。修改 Key 或其他环境变量后，必须重建容器：

```powershell
Set-Location "C:\Users\Patrick\Desktop\EchoMind所有代码+简历"

docker compose --project-directory .\EchoMind `
  -f .\EchoMind\docker-compose.yml `
  --env-file .\EchoMind\.env `
  up -d --force-recreate echomind nginx
```

### 5.2 修改 Python 后端代码

```powershell
docker compose --project-directory .\EchoMind `
  -f .\EchoMind\docker-compose.yml `
  --env-file .\EchoMind\.env `
  up -d --build echomind nginx
```

### 5.3 修改前端代码

```powershell
Set-Location "C:\Users\Patrick\Desktop\EchoMind所有代码+简历\EchoMindFrontend"
npm run build
docker compose up -d --build
```

## 6. 检查服务状态

### 6.1 查看后端容器

```powershell
docker compose --project-directory .\EchoMind `
  -f .\EchoMind\docker-compose.yml `
  ps
```

正常时以下服务应显示 `healthy`：

- `echomind-app`
- `echomind-redis`
- `echomind-chromadb`
- `echomind-prometheus`
- `echomind-nginx`

### 6.2 查看前端容器

```powershell
docker compose --project-directory .\EchoMindFrontend `
  -f .\EchoMindFrontend\docker-compose.yml `
  ps
```

### 6.3 健康检查

```powershell
Invoke-RestMethod "http://localhost:8003/health"
Invoke-RestMethod "http://localhost/health"
Invoke-RestMethod "http://localhost:5174/api/python/health"
Invoke-RestMethod "http://localhost:5174/api/python/knowledge/stats"
```

## 7. 查看日志

### Python 后端日志

```powershell
docker compose --project-directory .\EchoMind `
  -f .\EchoMind\docker-compose.yml `
  logs -f --tail 100 echomind
```

### 前端 Nginx 日志

```powershell
docker compose --project-directory .\EchoMindFrontend `
  -f .\EchoMindFrontend\docker-compose.yml `
  logs -f --tail 100 echomind-frontend
```

按 `Ctrl+C` 退出日志跟踪，不会停止容器。

## 8. 停止服务

### 仅停止，保留容器和数据

```powershell
docker compose --project-directory .\EchoMindFrontend `
  -f .\EchoMindFrontend\docker-compose.yml `
  stop

docker compose --project-directory .\EchoMind `
  -f .\EchoMind\docker-compose.yml `
  stop
```

### 删除容器，保留数据卷

```powershell
docker compose --project-directory .\EchoMindFrontend `
  -f .\EchoMindFrontend\docker-compose.yml `
  down

docker compose --project-directory .\EchoMind `
  -f .\EchoMind\docker-compose.yml `
  down
```

不要随意使用 `docker compose down -v`，因为 `-v` 会一起删除 Redis、ChromaDB 和 Prometheus 数据卷。

## 9. 常见问题

### 前端显示 502 Bad Gateway

1. 确认页面已选择 `Python`。
2. 访问 <http://localhost/health> 确认后端 Nginx 可用。
3. 查看后端容器状态和日志。

### 对话返回“请稍后重试”或日志出现 401

DeepSeek API Key 未生效或已失效。修改 `EchoMind/.env` 后，不能只执行 `restart`，必须按第 5.1 节使用 `--force-recreate`。

### `8000` 端口被占用

当前项目已通过下列配置使用 `8003`：

```env
ECHOMIND_HOST_PORT=8003
```

不要为了使用 `8000` 而停止不相关容器。

### Docker 命令无法连接

启动 Docker Desktop，等待状态显示 Engine running，然后再执行启动命令。

